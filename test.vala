public class test {
    public static void main() {
        bool [,] bools = new bool [2,3];
        int l1 = bools.length[0];
        int l2 = bools.length[1];
        stdout.printf(l1.to_string());
        stdout.printf(l2.to_string());
    }



}
